# BetterRandomTeleport
(Spigot 1.8 to 1.16.3) BRTP

BRTP is a lightweight plugin to randomly teleport a player across the map. You can easily define the options of the teleportation through the config.yml
